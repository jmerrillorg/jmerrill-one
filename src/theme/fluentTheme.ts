import { webLightTheme } from "@fluentui/react-components";

/**
 * JM1 Fluent Theme (Phase 2)
 * --------------------------------------------------
 * Canon rule:
 * - Phase 2 uses stock Fluent themes only
 * - Custom BrandVariants are introduced later
 * - This avoids TypeScript + token drift
 */
export const jm1FluentTheme = webLightTheme;